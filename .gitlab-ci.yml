image: debian:latest

variables:
  DEBIAN_FRONTEND: noninteractive
  PACKAGES_FOLDER_NAME: $CI_PROJECT_DIR/packages

before_script:
  - apt-get update -qq
  - apt-get install -qq -y dkms debhelper > /dev/null 2>&1

stages:
  - build

packaging:
  stage: build
  only:
    refs:
      - master
  script:
    - make package
    - mkdir $PACKAGES_FOLDER_NAME
    - mv *.deb $PACKAGES_FOLDER_NAME/
  artifacts:
    name: tuxedo-wmi-${CI_COMMIT_REF_SLUG}
    when: on_success
    paths:
      - $PACKAGES_FOLDER_NAME/*
